name: carvalho_app
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
    

  cupertino_icons: ^0.1.2
  mdi: ^1.0.1
  flutter_modular:
  mobx:
  flutter_mobx:
  firebase_core: ^0.5.0
  firebase_messaging: ^7.0.2
  firebase_analytics: ^5.0.11
  firebase_in_app_messaging: ^0.1.2
  cloud_firestore: ^0.14.1+2

dev_dependencies:
  build_runner:
  mobx_codegen:
  flutter_test:
    sdk: flutter
  url_launcher: ^5.4.2
  json_serializable: ^3.2.5

flutter:


  uses-material-design: true

  assets:
   - assets/images/image.jpg
   - assets/images/app_portifolio.png
   - assets/images/card.png